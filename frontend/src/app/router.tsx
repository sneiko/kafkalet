import { MainPage } from '@pages/main'

// Simple single-page app router — navigation handled inside MainPage with view state.
// Will be replaced by a real router (e.g. react-router) when multi-page needs arise.
export function Router() {
  return <MainPage />
}
